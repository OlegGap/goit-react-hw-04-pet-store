import React from 'react';
import styles from './Pet.module.css';
import moduleName from '../../../db/pets.json';

const Navigation = props => {
  console.log(props);

  return <>Some pet</>;
};
export default Navigation;
